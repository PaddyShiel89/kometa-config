templates:
  Director:
    smart_filter:
      all:
        director: tmdb
      sort_by: release.asc
    tmdb_person: <<person>>
    collection_mode: hide
    sort_title: "!16_<<collection_name>>"
    sync_mode: sync
