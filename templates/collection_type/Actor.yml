templates:
  Actor:
    smart_filter:
      all:
        actor: tmdb
      sort_by: release.asc
    tmdb_person: <<person>>
    collection_mode: hide
    sort_title: "!12_<<collection_name>>"
    sync_mode: sync
