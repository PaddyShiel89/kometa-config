external_templates:
  - repo: templates/collection_type/Theme

# Note: All summary text is prepended with the library name.
collections:
  Based on a Videogame:
    template:
      - name: Theme
        poster_folder: "Movies"
        summary: "focused on videogames."
    smart_filter:
      all:
        label: "theme: videogames"
      sort_by: title.asc
