external_templates:
  - repo: templates/collection_type/Theme
  - repo: templates/source/IMDb

# Note: All summary text is prepended with the library type.
collections:
  Based on a Book:
    template:
      - name: Theme
        summary: "based, however loosely, on novels, novellas, and other written works of fiction."
    smart_filter:
      all:
        label: "theme: based on a book"
      sort_by: title.asc

  Based on a Comic Book:
    template:
      - name: Theme
        summary: "based, however loosely, on graphic novels."
    smart_filter:
      all:
        label: "theme: based on a comic book"
      sort_by: title.asc

  Based on a True Story:
    template:
      - name: Theme
        summary: "based, however loosely, on real events."
    smart_filter:
      all:
        label: "theme: based on a true story"
      sort_by: title.asc

  Based on a Videogame:
    template:
      - name: Theme
        summary: "based, however loosely, on videogames."
    smart_filter:
      all:
        label: "theme: based on a videogame"
      sort_by: title.asc

  Stephen King:
    template:
      - name: Theme
        summary: "that are adaptations of works created by horror author Stephen King."
    smart_filter:
      all:
        label: "theme: based on Stephen King"
      sort_by: title.asc
